name: CI

on:
  push:
    branches:
      - '**'
  pull_request:

env:
  MAVEN_OPTS: -Xmx2g

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Verify
        run: mvn verify -Dwagon.skip=true -Decuacion-lib.version=15.1.0 -Decuacion-util.version=4.7.2

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: >-
            ecuacion-splib-core/target/site/jacoco/jacoco.xml,
            ecuacion-splib-jpa/target/site/jacoco/jacoco.xml,
            ecuacion-splib-batch/target/site/jacoco/jacoco.xml,
            ecuacion-splib-web/target/site/jacoco/jacoco.xml,
            ecuacion-splib-web-jpa/target/site/jacoco/jacoco.xml,
            ecuacion-splib-rest/target/site/jacoco/jacoco.xml
          fail_ci_if_error: false
